# Core web framework
streamlit==1.33.0

# Web scraping, parsing, and HTTP requests
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
html5lib>=1.1

# Selenium WebDriver for JavaScript rendering
selenium>=4.15.0
webdriver-manager>=4.0.0

# Data manipulation and analysis
pandas>=2.1.0

# Interactive plotting and visualization
plotly>=5.17.0

# Optional: If you want to add Excel export functionality
openpyxl>=3.1.0

# System requirements notes:
# - A compatible version of Chrome/Chromium browser must be installed in the environment.
# - webdriver-manager handles the ChromeDriver installation automatically.
# - For headless operation on Linux, system dependencies for Chrome are required.
#   (e.g., via `apt-get install -y google-chrome-stable`)
